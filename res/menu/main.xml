<menu xmlns:android="http://schemas.android.com/apk/res/android" >

    <item
        android:id="@+id/action_save"
        android:icon="@drawable/ic_content_save"
        android:orderInCategory="1"
        android:showAsAction="always"
        android:title="Save"/>
    <item
        android:id="@+id/action_clear"
        android:icon="@drawable/ic_content_discard"
        android:orderInCategory="2"
        android:showAsAction="always"
        android:title="Clear"/>
    <item
        android:id="@+id/action_eraser"
        android:icon="@drawable/ic_eraser"
        android:orderInCategory="3"
        android:showAsAction="always"
        android:title="Eraser"/>
    <!-- <item -->
    <!-- android:id="@+id/action_size" -->
    <!-- android:orderInCategory="100" -->
    <!-- android:showAsAction="ifRoom" -->
    <!-- android:title="Size"/> -->
    <!-- <item -->
    <!-- android:id="@+id/action_color" -->
    <!-- android:orderInCategory="100" -->
    <!-- android:showAsAction="ifRoom" -->
    <!-- android:title="Color"/> -->
    <item
        android:id="@+id/action_drawing_options"
        android:icon="@drawable/ic_brush_settings"
        android:orderInCategory="4"
        android:showAsAction="always"
        android:title="@string/brush_settings"/>

</menu>